def main():
    N = input()
    num = int(N)
    len = len(N)

    for i in N:
    recursion(N, len)
        for j in range(len):

def recursion(N, len, num):
    # Base Case
    if num == 0:
        c.append(count)

    # Recursive
    for i in range(len(N)):
        if N[i] != 0:
            num = num - N[i]

main()
