V, N, M = int(input()).split

A, B, T, P = [], [], [], []
for i in range(M):
    entrada = list(map(int, input().split))
    A[i] = entrada[0]
    B[i] = entrada[1]
    T[i] = entrada[2]
    P[i] = entrada[3]

X, Y = int(input().split)
