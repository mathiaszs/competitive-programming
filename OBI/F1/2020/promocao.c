#include <stdio.h>

typedef struct sllist
{
    int a
    struct sllist* next;
}
sllnode;

int main(void)
{
    int n;
    scanf("%d", &n);
    int a[n], b[n], e[n];

    for (int i = 1; i < n; i++)
    {
        scanf("%d%d%d", &)
    }

}
