letter = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J']

for a in range(10):
    for b in range(10):
        for c in range(10):
            for d in range(10):
                for e in range(10):
                    for f in range(10):
                        print(f"{ letter[a] }{ letter[b] }{ letter[c] }{ letter[d] }{ letter[e] }{ letter[f] }")
